import Accordion from "@/components/accordion";
import React from "react";

export const metadata = {
	title: "MateMatch Accordion ",
};

const index = () => {
	return (
		<>
			<Accordion />
		</>
	);
};

export default index;
