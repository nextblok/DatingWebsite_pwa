import React from "react";
import Alerts from "@/components/alerts";

export const metadata = {
	title: "MateMatch Alerts ",
};

const index = () => {
	return (
		<>
			<Alerts />
		</>
	);
};

export default index;
