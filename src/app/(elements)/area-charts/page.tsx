import AreaCharts from "@/components/area-charts";
import React from "react";

export const metadata = {
	title: "MateMatch Area Charts ",
};

const index = () => {
	return (
		<>
			<AreaCharts />
		</>
	);
};

export default index;
