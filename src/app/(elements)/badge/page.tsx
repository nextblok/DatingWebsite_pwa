import Badge from "@/components/badge";
import React from "react";

export const metadata = {
	title: "MateMatch Badge ",
};

const index = () => {
	return (
		<>
			<Badge />
		</>
	);
};

export default index;
