import BootstrapCarousel from "@/components/bootstrap-carousel";
import React from "react";

export const metadata = {
	title: "MateMatch Bootstrap Carousel ",
};

const index = () => {
	return (
		<>
			<BootstrapCarousel />
		</>
	);
};

export default index;
