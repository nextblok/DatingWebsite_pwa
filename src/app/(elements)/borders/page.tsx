import React from "react";
import Borders from "@/components/borders";

export const metadata = {
	title: "MateMatch Borders ",
};

const index = () => {
	return (
		<>
			<Borders />
		</>
	);
};

export default index;
