import React from "react";
import Breadcrumb from "@/components/breadcrumb";

export const metadata = {
	title: "MateMatch Breadcrumb ",
};

const index = () => {
	return (
		<>
			<Breadcrumb />
		</>
	);
};

export default index;
