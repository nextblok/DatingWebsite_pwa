import Card from "@/components/card";
import React from "react";

export const metadata = {
	title: "MateMatch Card ",
};

const index = () => {
	return (
		<>
			<Card />
		</>
	);
};

export default index;
