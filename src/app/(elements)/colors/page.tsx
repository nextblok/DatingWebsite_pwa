import Colors from "@/components/colors";
import React from "react";

export const metadata = {
	title: "MateMatch Colors ",
};

const index = () => {
	return (
		<>
			<Colors />
		</>
	);
};

export default index;
