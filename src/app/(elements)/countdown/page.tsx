import Countdown from "@/components/countdown";
import React from "react";

export const metadata = {
	title: "MateMatch Count Down ",
};

const index = () => {
	return (
		<>
			<Countdown />
		</>
	);
};

export default index;
