import Counterup from "@/components/counterup";
import React from "react";

export const metadata = {
	title: "MateMatch Counter Up ",
};

const index = () => {
	return (
		<>
			<Counterup />
		</>
	);
};

export default index;
