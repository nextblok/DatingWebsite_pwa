import DataTable from "@/components/data-table";
import React from "react";

export const metadata = {
	title: "MateMatch Data Table ",
};

const index = () => {
	return (
		<>
			<DataTable />
		</>
	);
};

export default index;
