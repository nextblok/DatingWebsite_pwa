import React from "react";
import FooterMenu from "@/components/footer-menu";

export const metadata = {
	title: "MateMatch Footer Menu ",
};

const index = () => {
	return (
		<>
			<FooterMenu />
		</>
	);
};

export default index;
