import FormAutoComplete from "@/components/form-auto-complete";
import React from "react";

export const metadata = {
	title: "MateMatch Form Auto Complete ",
};

const index = () => {
	return (
		<>
			<FormAutoComplete />
		</>
	);
};

export default index;
