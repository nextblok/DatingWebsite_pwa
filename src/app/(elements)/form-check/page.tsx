import FormCheck from "@/components/form-check";
import React from "react";

export const metadata = {
	title: "MateMatch Form Check ",
};

const index = () => {
	return (
		<>
			<FormCheck />
		</>
	);
};

export default index;
