import React from "react";
import FormInputGroup from "@/components/form-input-group";

export const metadata = {
	title: "MateMatch Form Input Group ",
};

const index = () => {
	return (
		<>
			<FormInputGroup />
		</>
	);
};

export default index;
