import React from "react";
import FormInput from "@/components/form-input";

export const metadata = {
	title: "MateMatch Form Input ",
};

const index = () => {
	return (
		<>
			<FormInput />
		</>
	);
};

export default index;
