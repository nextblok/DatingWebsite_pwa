import FormRadio from "@/components/form-radio";
import React from "react";

export const metadata = {
	title: "MateMatch Form Radio ",
};

const index = () => {
	return (
		<>
			<FormRadio />
		</>
	);
};

export default index;
