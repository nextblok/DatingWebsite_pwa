import FormRange from "@/components/form-range";
import React from "react";

export const metadata = {
	title: "MateMatch Form Range ",
};

const index = () => {
	return (
		<>
			<FormRange />
		</>
	);
};

export default index;
