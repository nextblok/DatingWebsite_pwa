import React from "react";
import FormSelect from "@/components/form-select";

export const metadata = {
	title: "MateMatch Form Select ",
};

const index = () => {
	return (
		<>
			<FormSelect />
		</>
	);
};

export default index;
