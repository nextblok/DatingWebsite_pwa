import FormSwitches from "@/components/form-switches";
import React from "react";

export const metadata = {
	title: "MateMatch Form Switches ",
};

const index = () => {
	return (
		<>
			<FormSwitches />
		</>
	);
};

export default index;
