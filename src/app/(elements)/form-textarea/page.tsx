import React from "react";
import FormTextarea from "@/components/form-textarea";

export const metadata = {
	title: "MateMatch Form Textarea ",
};

const index = () => {
	return (
		<>
			<FormTextarea />
		</>
	);
};

export default index;
