import FormValidation from "@/components/form-validation";
import React from "react";

export const metadata = {
	title: "MateMatch Form Validation ",
};

const index = () => {
	return (
		<>
			<FormValidation />
		</>
	);
};

export default index;
