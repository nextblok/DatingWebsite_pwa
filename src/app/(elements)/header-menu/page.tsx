import React from "react";
import HeaderMenu from "@/components/header-menu";

export const metadata = {
	title: "MateMatch Header Menu ",
};

const index = () => {
	return (
		<>
			<HeaderMenu />
		</>
	);
};

export default index;
