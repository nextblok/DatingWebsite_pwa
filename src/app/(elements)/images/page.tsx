import Images from "@/components/images";
import React from "react";

export const metadata = {
	title: "MateMatch Images ",
};

const index = () => {
	return (
		<>
			<Images />
		</>
	);
};

export default index;
