import LineCharts from "@/components/line-charts";
import React from "react";

export const metadata = {
	title: "MateMatch Line Charts ",
};

const index = () => {
	return (
		<>
			<LineCharts />
		</>
	);
};

export default index;
