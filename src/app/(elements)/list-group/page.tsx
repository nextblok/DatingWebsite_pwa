import ListGroup from "@/components/list-group";
import React from "react";

export const metadata = {
	title: "MateMatch List Group ",
};

const index = () => {
	return (
		<>
			<ListGroup />
		</>
	);
};

export default index;
