import React from "react";
import Modal from "@/components/modal";

export const metadata = {
	title: "MateMatch Modal ",
};

const index = () => {
	return (
		<>
			<Modal />
		</>
	);
};

export default index;
