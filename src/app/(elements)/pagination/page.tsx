import Pagination from "@/components/pagination";
import React from "react";

export const metadata = {
	title: "MateMatch Pagination ",
};

const index = () => {
	return (
		<>
			<Pagination />
		</>
	);
};

export default index;
