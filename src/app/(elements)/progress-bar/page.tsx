import ProgressBar from "@/components/progress-bar";
import React from "react";

export const metadata = {
	title: "MateMatch Progress Bar ",
};

const index = () => {
	return (
		<>
			<ProgressBar />
		</>
	);
};

export default index;
