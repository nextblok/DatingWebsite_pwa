import Scrollspy from "@/components/scrollspy";
import React from "react";

export const metadata = {
	title: "MateMatch Scrollspy ",
};

const index = () => {
	return (
		<>
			<Scrollspy />
		</>
	);
};

export default index;
