import Shadows from "@/components/shadows";
import React from "react";

export const metadata = {
	title: "MateMatch Shadows ",
};

const index = () => {
	return (
		<>
			<Shadows />
		</>
	);
};

export default index;
