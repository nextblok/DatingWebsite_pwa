import React from "react";
import SidebarLeftMenu from "@/components/sidebar-left-menu";

export const metadata = {
	title: "MateMatch Sidebar Left Menu ",
};

const index = () => {
	return (
		<>
			<SidebarLeftMenu />
		</>
	);
};

export default index;
