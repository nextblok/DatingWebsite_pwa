import React from "react";
import SidebarRightMenu from "@/components/sidebar-right-menu";

export const metadata = {
	title: "MateMatch Sidebar Right Menu ",
};

const index = () => {
	return (
		<>
			<SidebarRightMenu />
		</>
	);
};

export default index;
