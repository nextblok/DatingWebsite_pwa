import Sizing from "@/components/sizing";
import React from "react";

export const metadata = {
	title: "MateMatch Sizing ",
};

const index = () => {
	return (
		<>
			<Sizing />
		</>
	);
};

export default index;
