import Text from "@/components/text";
import React from "react";

export const metadata = {
	title: "MateMatch Text ",
};

const index = () => {
	return (
		<>
			<Text />
		</>
	);
};

export default index;
