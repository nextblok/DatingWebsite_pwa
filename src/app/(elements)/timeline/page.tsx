import React from "react";
import Timeline from "@/components/timeline";

export const metadata = {
	title: "MateMatch Timeline ",
};

const index = () => {
	return (
		<>
			<Timeline />
		</>
	);
};

export default index;
