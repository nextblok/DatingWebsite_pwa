import Tooltips from "@/components/tooltips";
import React from "react";

export const metadata = {
	title: "MateMatch Tooltips ",
};

const index = () => {
	return (
		<>
			<Tooltips />
		</>
	);
};

export default index;
