import Typography from "@/components/typography";
import React from "react";

export const metadata = {
	title: "MateMatch Typography ",
};

const index = () => {
	return (
		<>
			<Typography />
		</>
	);
};

export default index;
