import UserRatings from "@/components/user-ratings";
import React from "react";

export const metadata = {
	title: "MateMatch User Ratings ",
};

const index = () => {
	return (
		<>
			<UserRatings />
		</>
	);
};

export default index;
