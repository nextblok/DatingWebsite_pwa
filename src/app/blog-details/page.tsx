import BlogDetails from "@/components/blog-details";
import React from "react";

export const metadata = {
	title: "MateMatch Blog Details ",
};

const index = () => {
	return (
		<>
			<BlogDetails />
		</>
	);
};

export default index;
