import React from "react";
import BlogGrid from "@/components/blog-grid";

export const metadata = {
	title: "MateMatch Blog Grid ",
};

const index = () => {
	return (
		<>
			<BlogGrid />
		</>
	);
};

export default index;
