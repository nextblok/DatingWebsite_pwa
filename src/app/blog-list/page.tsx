import BlogList from "@/components/blog-list";
import React from "react";

export const metadata = {
	title: "MateMatch Blog List ",
};

const index = () => {
	return (
		<>
			<BlogList />
		</>
	);
};

export default index;
