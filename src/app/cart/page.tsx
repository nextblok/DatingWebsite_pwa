import Cart from "@/components/cart";
import React from "react";

export const metadata = {
	title: "MateMatch Cart ",
};

const index = () => {
	return (
		<>
			<Cart />
		</>
	);
};

export default index;
