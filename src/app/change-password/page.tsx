import React from "react";
import ChangePassword from "@/components/change-password";

export const metadata = {
	title: "MateMatch Change Password ",
};

const index = () => {
	return (
		<>
			<ChangePassword />
		</>
	);
};

export default index;
