import ChatUsers from "@/components/chat-users";
import React from "react";

export const metadata = {
	title: "MateMatch Chat Users ",
};

const index = () => {
	return (
		<>
			<ChatUsers />
		</>
	);
};

export default index;
