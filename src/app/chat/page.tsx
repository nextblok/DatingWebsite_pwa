import Chat from "@/components/chat";
import React from "react";

export const metadata = {
	title: "MateMatch Chat ",
};

const index = () => {
	return (
		<>
			<Chat />
		</>
	);
};

export default index;
