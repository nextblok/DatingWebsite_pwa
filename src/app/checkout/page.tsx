import React from "react";
import Checkout from "@/components/checkout";

export const metadata = {
	title: "MateMatch Checkout ",
};

const index = () => {
	return (
		<>
			<Checkout />
		</>
	);
};

export default index;
