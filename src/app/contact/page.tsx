import React from "react";
import Contact from "@/components/contact";

export const metadata = {
	title: "MateMatch Contact ",
};

const index = () => {
	return (
		<>
			<Contact />
		</>
	);
};

export default index;
