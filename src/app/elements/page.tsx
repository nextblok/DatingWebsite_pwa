import React from "react";
import Elements from "@/components/elements";

export const metadata = {
	title: "MateMatch Elements ",
};

const index = () => {
	return (
		<>
			<Elements />
		</>
	);
};

export default index;
