import Fallback from "@/components/fallback";
import React from "react";

export const metadata = {
	title: "MateMatch Fallback ",
};

const index = () => {
	return (
		<>
			<Fallback />
		</>
	);
};

export default index;
