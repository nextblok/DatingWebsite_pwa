import ForgetPassword from "@/components/forget-password";
import React from "react";

export const metadata = {
	title: "MateMatch Forget Password ",
};

const index = () => {
	return (
		<>
			<ForgetPassword />
		</>
	);
};

export default index;
