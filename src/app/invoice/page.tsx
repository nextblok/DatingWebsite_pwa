import React from "react";
import Invoice from "@/components/invoice";

export const metadata = {
	title: "MateMatch Invoice ",
};

const index = () => {
	return (
		<>
			<Invoice />
		</>
	);
};

export default index;
