import Language from "@/components/language";
import React from "react";

export const metadata = {
	title: "MateMatch Language ",
};

const index = () => {
	return (
		<>
			<Language />
		</>
	);
};

export default index;
