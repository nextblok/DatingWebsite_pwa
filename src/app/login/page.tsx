import Login from "@/components/login";
import React from "react";

export const metadata = {
	title: "MateMatch Login ",
};

const index = () => {
	return (
		<>
			<Login />
		</>
	);
};

export default index;
