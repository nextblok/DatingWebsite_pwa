import Membership from "@/components/membership";
import React from "react";

export const metadata = {
	title: "MateMatch Membership ",
};

const index = () => {
	return (
		<>
			<Membership />
		</>
	);
};

export default index;
