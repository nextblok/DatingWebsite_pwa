import React from "react";
import NotificationDetails from "@/components/notification-details";

export const metadata = {
	title: "MateMatch Notification Details ",
};

const index = () => {
	return (
		<>
			<NotificationDetails />
		</>
	);
};

export default index;
