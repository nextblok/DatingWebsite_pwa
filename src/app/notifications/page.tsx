import React from "react";
import Notifications from "@/components/notifications";

export const metadata = {
	title: "MateMatch Notifications ",
};

const index = () => {
	return (
		<>
			<Notifications />
		</>
	);
};

export default index;
