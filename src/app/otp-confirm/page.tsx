import OtpConfirm from "@/components/otp-confirm";
import React from "react";

export const metadata = {
	title: "MateMatch Otp Confirm ",
};

const index = () => {
	return (
		<>
			<OtpConfirm />
		</>
	);
};

export default index;
