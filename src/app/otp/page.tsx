import Otp from "@/components/otp";
import React from "react";

export const metadata = {
	title: "MateMatch Otp ",
};

const index = () => {
	return (
		<>
			<Otp />
		</>
	);
};

export default index;
