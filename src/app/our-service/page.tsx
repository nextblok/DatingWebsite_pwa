import React from "react";
import OurService from "@/components/our-service";

export const metadata = {
	title: "MateMatch Our Service ",
};

const index = () => {
	return (
		<>
			<OurService />
		</>
	);
};

export default index;
