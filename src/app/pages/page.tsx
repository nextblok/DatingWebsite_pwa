import Pages from "@/components/pages";
import React from "react";

export const metadata = {
	title: "MateMatch Pages ",
};

const index = () => {
	return (
		<>
			<Pages />
		</>
	);
};

export default index;
