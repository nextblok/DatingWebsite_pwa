import React from "react";
import PaymentConfirm from "@/components/payment-confirm";

export const metadata = {
	title: "MateMatch Payment Confirm ",
};

const index = () => {
	return (
		<>
			<PaymentConfirm />
		</>
	);
};

export default index;
