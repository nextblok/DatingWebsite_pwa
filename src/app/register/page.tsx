import Register from "@/components/register";
import React from "react";

export const metadata = {
	title: "MateMatch Register ",
};

const index = () => {
	return (
		<>
			<Register />
		</>
	);
};

export default index;
