import React from "react";
import SearchResult from "@/components/search-result";

export const metadata = {
	title: "MateMatch Search Result ",
};

const index = () => {
	return (
		<>
			<SearchResult />
		</>
	);
};

export default index;
