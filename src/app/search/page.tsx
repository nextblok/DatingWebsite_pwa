import React from "react";
import Search from "@/components/search";

export const metadata = {
	title: "MateMatch Search",
};

const index = () => {
	return (
		<>
			<Search />
		</>
	);
};

export default index;
