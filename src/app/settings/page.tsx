import Settings from "@/components/settings";
import React from "react";

export const metadata = {
	title: "MateMatch Settings ",
};

const index = () => {
	return (
		<>
			<Settings />
		</>
	);
};

export default index;
