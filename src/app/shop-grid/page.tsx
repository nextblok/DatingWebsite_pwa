import ShopGrid from "@/components/shop-grid";
import React from "react";

export const metadata = {
	title: "MateMatch Shop Grid ",
};

const index = () => {
	return (
		<>
			<ShopGrid />
		</>
	);
};

export default index;
