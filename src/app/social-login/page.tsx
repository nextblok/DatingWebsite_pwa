import SocialLogin from "@/components/social-login";
import React from "react";

export const metadata = {
	title: "MateMatch Social Login ",
};

const index = () => {
	return (
		<>
			<SocialLogin />
		</>
	);
};

export default index;
