import React from "react";
import UserProfile from "@/components/user-profile";

export const metadata = {
	title: "MateMatch User Profile ",
};

const index = () => {
	return (
		<>
			<UserProfile />
		</>
	);
};

export default index;
