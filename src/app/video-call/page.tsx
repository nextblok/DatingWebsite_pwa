import VideoCall from "@/components/video-call";
import React from "react";

export const metadata = {
	title: "MateMatch Video Call ",
};

const index = () => {
	return (
		<>
			<VideoCall />
		</>
	);
};

export default index;
