"use client"
import React, {
	useEffect,
	useState,
	useRef,
	Suspense,
	useCallback,
	useMemo,
} from "react";
import ChatAreaNew from "./ChatAreaNew";

const Chat = () => {
	return (
		<>
			<ChatAreaNew />
		</>
	);
};

export default Chat;
