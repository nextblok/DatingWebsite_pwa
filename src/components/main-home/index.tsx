import Home from "@/components/home";
import React from "react";

export const metadata = {
	title: "MateMatch Home ",
};

const index = () => {
	return (
		<>
			<Home />
		</>
	);
};

export default index;
